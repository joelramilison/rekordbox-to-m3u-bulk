#ifndef CONNECT_DATA_C
#define CONNECT_DATA_C

#include "structs.h"

int findPathsByTags(char*** dest, size_t* capacity, char* title, char* artist, struct LocalTracksArray* local);

bool addPathsToCollection(struct LocalTracksArray* local, struct CollectionTracksArray* collection);









#endif